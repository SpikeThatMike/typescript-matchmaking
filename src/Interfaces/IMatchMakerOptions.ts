export default interface IMatchMakerOptions {
    minPlayers: number;
    maxPlayers: number;
    minTeams: number;
    maxTeams: number;
    minTeamSize: number;
    maxTeamSize: number;
    ratingRange: number;
    ratingTeamRange: number;
}